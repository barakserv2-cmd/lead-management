"use client";

// Placeholder hook for lead data fetching.
// Will use Supabase realtime subscriptions once connected.
export function useLeads() {
  return {
    leads: [],
    loading: false,
    error: null,
  };
}
