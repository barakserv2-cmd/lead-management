-- ============================================================
-- Migration: האצלת שליחת מסמכים לחתימה
-- Description: doc_delegates על חשבון וואטסאפ — רשימת אימיילים
--              של רכזות שמורשות לשלוח מסמכים לחתימה דרך המספר
--              הזה, בלי בעלות על החשבון: לא רואות את השיחות שלו
--              ולא שולחות הודעות ידניות. משמש רק את זרימת
--              החתימה הדיגיטלית (signatureSend).
-- ============================================================

ALTER TABLE whatsapp_accounts
  ADD COLUMN IF NOT EXISTS doc_delegates JSONB NOT NULL DEFAULT '[]';
